<UserControl x:Class="RTDicomViewer.View.MainWindow.DoseObjectDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RTDicomViewer.View.MainWindow"
             xmlns:ex="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             DataContext="{Binding DoseObjectDisplay, Source={StaticResource Locator}}"
              >
    <TreeView ItemsSource="{Binding DoseObjects}" Style="{StaticResource {x:Type TreeView}}" >
        <TreeView.ItemTemplate>
            <DataTemplate>
                <WrapPanel>
                    <CheckBox Margin="0" Content="{Binding Value.Name}" IsChecked="{Binding IsSelected}"  BorderThickness="1"/>
                </WrapPanel>
            </DataTemplate>
        </TreeView.ItemTemplate>
    <TreeView.ItemContainerStyle>
        <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
            <!--<Setter Property="ContextMenu" Value="{StaticResource DoseContextMenu}"/>-->
        </Style>
        </TreeView.ItemContainerStyle>
        

    </TreeView>
</UserControl>
