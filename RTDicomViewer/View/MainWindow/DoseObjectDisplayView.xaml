<UserControl x:Class="RTDicomViewer.View.MainWindow.DoseObjectDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RTDicomViewer.View.MainWindow"
             xmlns:ex="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             DataContext="{Binding DoseObjectDisplay, Source={StaticResource Locator}}"
              >

    <GroupBox Header="Dose">
        <StackPanel>
            <ListView Height="120" ItemsSource="{Binding DoseObjects}" SelectedItem="{Binding SelectedDose}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel>
                            <CheckBox Margin="0 0 2 0" IsChecked="{Binding IsSelected}"  BorderThickness="1"/>
                            <TextBlock Text="{Binding Value.Name}"/>
                        </WrapPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <!--<Setter Property="ContextMenu" Value="{StaticResource DoseContextMenu}"/>-->
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <GroupBox Header="Properties">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">Name:</TextBlock>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedDose.Value.Name, UpdateSourceTrigger=PropertyChanged}"></TextBox>

                    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Scaling:</TextBlock>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDose.Value.DoseGridScaling}" IsEnabled="False"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">Units:</TextBlock>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDose.Value.Units}" IsEnabled="False"/>
                </Grid>
            </GroupBox>
            <GroupBox Header="Display">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0">
                        <ItemsControl ItemsSource="{Binding SelectedDose.Children}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Value}"></CheckBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    
                    <TextBlock Text="LUT:" Grid.Row="3" Grid.Column="0"></TextBlock>
                    <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding LUTs}" SelectedItem="{Binding SelectedRenderableImage.LUT}"></ComboBox>

                </Grid>
            </GroupBox>
        </StackPanel>
    </GroupBox>
</UserControl>
