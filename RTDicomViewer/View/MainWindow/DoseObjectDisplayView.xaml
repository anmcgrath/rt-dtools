<UserControl x:Class="RTDicomViewer.View.MainWindow.DoseObjectDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RTDicomViewer.View.MainWindow"
             xmlns:ex="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding DoseObjectDisplay, Source={StaticResource Locator}}">
    <TreeView>
        <TreeViewItem ItemsSource="{Binding DoseObjects}" Header="Dose grids">
            <TreeViewItem.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <CheckBox Margin="0" Content="{Binding Name}" Command="{Binding SelectDoseCommand}" CommandParameter="{Binding .}" IsChecked="{Binding IsSelected}"  BorderThickness="1"/>
                    </WrapPanel>
                </DataTemplate>
            </TreeViewItem.ItemTemplate>
            <TreeViewItem.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <!--<Setter Property="ContextMenu" Value="{StaticResource DoseContextMenu}"/>-->
                </Style>
            </TreeViewItem.ItemContainerStyle>
        </TreeViewItem>
    </TreeView>
</UserControl>
